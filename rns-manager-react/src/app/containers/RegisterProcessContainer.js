import { connect } from 'react-redux';
import { RegisterProcessComponent } from '../components';

const mapStateToProps = () => ({

});

export default connect(mapStateToProps)(RegisterProcessComponent);
