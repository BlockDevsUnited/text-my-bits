import { CONTENT, CHAIN_ADDR } from './types';
import filedActions from '../../factories/actionFactory';

export const content = filedActions(CONTENT);
export const chainAddr = filedActions(CHAIN_ADDR);
